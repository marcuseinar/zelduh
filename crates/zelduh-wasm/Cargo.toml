[package]
name = "zelduh-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The browser entry point: a plain wasm module with no JS bindings generator"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
zelduh-core = { path = "../zelduh-core" }
zelduh-assets = { path = "../zelduh-assets" }
zelduh-gen = { path = "../zelduh-gen" }
zelduh-render = { path = "../zelduh-render" }
