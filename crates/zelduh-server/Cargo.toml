[package]
name = "zelduh-server"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The multiplayer server: a WebSocket relay with an authoritative clock"

[[bin]]
name = "zelduh-server"
path = "src/main.rs"

[dependencies]
zelduh-core = { path = "../zelduh-core" }
zelduh-gen = { path = "../zelduh-gen" }
